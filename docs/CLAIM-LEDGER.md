# Claim ledger

Every load-bearing statement Haldir makes about itself has a claim id, a status,
and a pointer to the evidence that backs it — or an explicit note that it is
unproven, deferred, or out of scope. This ledger is the single place where claims
and evidence are reconciled; the drift guard `tools/verify-claims.py` treats any
doc line citing a `CL-` id as scoped, so overclaims must be entered here honestly
rather than asserted loosely elsewhere.

Scope: this repository implements the **P0 `assurance-reference-v1`** profile —
the repaired, in-process pure core. See `docs/ASSURANCE-PROFILES.md` for the
profile matrix and `docs/LIMITATIONS.md` for what P0 deliberately does not do.

## Status vocabulary

- **PROVEN** — backed by code plus an automated test or CI gate in this repo.
- **PARTIAL** — mechanism implemented; coverage or an external gate is still open.
- **PENDING** — implemented but its evidence gate has not yet run green in CI.
- **UNPROVEN** — asserted nowhere as an affirmative property; would require a
  capability (live transport, hardware, a neural backend) absent from P0.
- **OUT OF SCOPE** — intentionally deferred to a later profile (P1+); not a gap
  in P0.

## Proven in P0 (code + test/CI evidence)

| Claim | Statement | Status | Evidence |
| --- | --- | --- | --- |
| CL-CBOR-01 | Wire encoding is strict deterministic CBOR: integer keys, shortest ints, no floats/tags/indefinite, and every value re-encodes to identical bytes. | PROVEN | `haldir-contracts` canonical/malformed decoder tests + bounded hostile-byte property tests (`just parser-property-smoke`) |
| CL-COSE-01 | Every signed message is `COSE_Sign1`/Ed25519 with a domain-separated AAD (`{kind}.v{major}`) and a per-kind content type. Verification independently requires the signature nonce point to use a canonical prime-subgroup encoding before invoking the pinned provider's cofactored equation. | PROVEN | `haldir-crypto` COSE, known-answer, canonical/torsion nonce-point, and exact-byte tests |
| CL-TRUST-01 | Every trusted application-key record carries a mandatory canonical 1..=64-byte ASCII subject. Verification-key construction admits only the unique canonical encoding of a non-identity prime-subgroup point. The trust store admits an idempotent identical whole record, rejects a conflicting record for the same key id without mutation, and rejects enrollment of the same admitted point under a second key id regardless of proposed role, subject, or assurance class. This prevents missing/unbounded signer identity, noncanonical/torsion aliases, and ordinary cross-authority key reuse; it does not prove independent key generation, key custody, or that distinct admitted public keys have unrelated private keys. | PROVEN | `haldir-crypto` canonical/noncanonical/pure-torsion/mixed-torsion key tests plus bounded-subject and insertion-precedence/atomicity/cross-role/cross-subject/cross-class alias tests; residual provisioning requirements in `docs/AUTHORITY-GRAPH.md` |
| CL-ADMISSION-01 | The admission snapshot exposes only fallible insertion: before any lookup or mutation it validates the candidate's supported record schema, requires the available conformance-run digest for every cumulative A2-through-A6 claim, admits an idempotent identical valid record, rejects a conflicting record for the same admission id without changing the original binding, and bounds active records. Its revocation index is bounded and authoritative even when an inactive record is absent; each new id requires a strictly advancing epoch, an already-revoked id at or below the high-water is idempotent, and a higher issuer epoch advances the high-water without growing the set. Authority-loading callers therefore cannot silently admit an unsupported or unevidenced higher-level schema record or use a last-record-wins compatibility path. This proves the in-memory index invariants and one evidence-digest presence rule, not record-signature verification, authenticated loader provenance, an earned behavioural admission level or complete cumulative evidence chain, durable snapshot freshness, or external rollback resistance. | PROVEN | `haldir-admission` invalid/unevidenced-record, conflict/atomicity, record/revocation capacity, missing-active-record revocation, monotonic-epoch, and idempotency tests plus compile-time absence of an infallible insertion method |
| CL-CHALLENGE-01 | Gate challenges are bounded, session-bound, signed, and one-shot. The public actor surface issues a fresh OS-CSPRNG nonce under a fixed Gate-monotonic TTL, signs all challenge scope fields with the configured Gate application key, allocates a per-actor nonzero sequence, and exposes no raw nonce-registration method; the declared-live typestate instead consumes nonce entropy already obtained before durable startup effects. Internally, duplicate or already-expired registration is rejected; consumed and expired nonces retain bounded tombstones while releasing pending capacity; and retained-capacity or sequence exhaustion fails closed. Public lease acceptance validates the supported lease schema/semantics and rejects an invalid lease or an unbound/wrong-session table before any term-store lookup or challenge mutation. The accepted runtime snapshot preserves the contract's bounded/nonzero authority types rather than widening them. Session binding is immutable for a table's lifetime, so rollover requires replacing the owning actor/table rather than an A→B→A rebind. | PROVEN | `haldir-state` `challenge::tests` plus `model::invalid_lease_spends_no_authority_state` and `model::challenge_session_mismatch_has_no_term_or_challenge_side_effects`; `haldir-gate` signed actor/live challenge tests and public API compile checks; typed `ActiveMissionLeaseSnapshot`; Gate construction binds its table to the configured session |
| CL-GATE-MEDIATION-01 | Within P0, no unauthorized, misscoped, replayed, or malformed intent produces an accepted plant command; only a fully valid intent drives the reference plant. Schema-v1 auxiliary input watermarks are prohibited because no authentication/freshness/policy semantics exist for them. A policy denial retains the complete fixed-order, distinct, bounded denial vector in the Gate-signed receipt instead of silently selecting its first member. This is the P0 conjunction over the declared in-process route, not complete mediation of a real vehicle. | PROVEN | `haldir-contracts` intent/receipt semantic tests; `haldir-gate` signed-watermark and multi-denial receipt tests; `haldir-range` adversarial campaign |
| CL-REFERENCE-PLANT-CORRELATION-01 | The in-process reference plant is bound to an independently supplied exact NCP session before it observes any command; a wrong first frame is rejected and cannot rebind that receiver state. It accepts a `PlantCommand` only through a profile-checked, self-consistent immutable `ExactNcpCommandFrame`; session, output epoch/sequence, causal-source correlation, action, validity, and output-frame digest are derived from that single object rather than independently supplied command fields. Every command-related Received/Validated/Accepted/Selected/Applied/ResponseObserved/Expired/Rejected model event carries one atomic exact-object session/output/source/digest correlation. The digest binds the serialized source epoch/sequence in both profiles and also binds `source_key` in modeled P0; exact NCP v0.8 JSON has no `source_key` field, so that key and the decision id are explicitly in-process correlation only there and are not authenticated by the output bytes. At the latest accepted output position, an exact replay and a conflicting frame are deterministically distinguished; both, and every older position, are rejected without refreshing active authority or expiry. A different output epoch cannot replace a live stream; it becomes eligible only at the prior horizon's exact half-open expiry boundary, at which point the prior epoch is permanently retired. Fixed-point position integration carries signed sub-millimetre remainder across ticks instead of repeatedly erasing low-speed displacement, and fractional representation overflow is transactional. Failed capacity/time/tombstone preflight remains transactional. This proves deterministic in-memory model correlation, not authenticated session provisioning, authenticated publisher identity, per-position historical conflict retention, transport provenance, delivery, receiver authentication, physical application, or serialized plant evidence. | PROVEN | `haldir-reference-plant` configured-session-before-first-command, exact-correlation lifecycle/rejection, duplicate, conflicting-source, conflicting-byte, live-prior-stream/exact-expiry transition, sub-millimetre integration, fractional-boundary, capacity, time, and tombstone transaction tests; `haldir-ncp08` modeled/real profile self-consistency, source-key projection, and tamper tests; `haldir-gate` exact-frame-derived PlantCommand construction |
| CL-REFERENCE-PLANT-DYNAMICS-01 | The fixed-tick reference model never rounds command validity up: only complete ticks covered by the declared validity may select and apply a command. Both normal acceleration and safe-action deceleration bound the Euclidean magnitude of each integer velocity-vector delta, using checked widened arithmetic and deterministic scaling rather than applying the configured bound independently on every axis. This proves those discrete in-memory simulation semantics, not real-time scheduling, continuous-time dynamics, actuator fidelity, or physical safety. | PROVEN | `haldir-reference-plant` `validity_uses_only_complete_ticks_without_extending_authority`, `normal_acceleration_is_bounded_by_vector_magnitude`, `safe_deceleration_is_bounded_by_vector_magnitude`, vector-approach determinism/boundary tests, and transactional arithmetic tests |
| CL-AUTHORITY-MODEL-01 | The Haldir 0.9 normative authority model operationally counts any exact final-route frame intended for plant consumption as a plant command, including an unauthorized bypass. It permits only the logical Gate application/transport principal to construct and publish a Gate-authored command after the full conjunction, keeps `ALLOW`/`DENY`/`ERROR` decisions distinct from `HOLD`/`VELOCITY_LOCAL_NED` actions, and records implementation enforcement as PARTIAL. The machine verifier cross-checks the document digest, closed JSON vocabulary, secure-reference publisher/receiver matrix, retained synthetic ACL cases, current Rust vocabularies and construction order, the runtime decision/action regression, and the serial requirement ledger. This proves a frozen, internally consistent rule and local drift detection—not sealed lower APIs, credential custody, global publisher exclusivity, bypass closure, complete mediation, delivery, application, or physical safety. | PROVEN | `docs/release/0.9.0/AUTHORITY-CONTRACT.md`; `release/0.9.0/authority-model.json`; `haldir-gate` `authority_contract_separates_hold_action_from_negative_decisions`; `tools/release/test_verify_authority_model.py`; `tools/release/verify-authority-model.py` |
| CL-PROTECTION-MODEL-01 | The Haldir 0.9 protection model freezes a default-deny inventory of transport principals/CNs, exact application-role to key-class/object/domain/subject bindings, logical subject/type mappings, exact profile routes, separately custodied secrets and state resources, protected operations, authorization constraints, time domains, trust roots and their evidence status. It makes Gate boot-local monotonic time the only hot-path authority clock, treats controller/source timestamps as provenance only, separates opaque identities, boot/epoch-scoped counters, and same-scope cross-boot ratchets, and prevents role-name, advisory-evidence, command-shape, or controller-field inference from creating trust. The bounded verifier rejects missing/duplicate/unknown entries, unknown or drifted ownership/custody, wildcard/default-allow grants, access-tuple or role/domain drift, any non-Gate command escalation, final-constraint removal, controller-derived roots, provenance-clock escalation, T001 vocabulary drift, and an unverified predecessor. This proves a closed executable definition and local drift detection with implementation enforcement as PARTIAL—not protected credential custody, mandatory package consumption, authenticated live inputs, sealed capability/global handle exclusivity, external anti-rewind, complete mediation, delivery, plant application, physical safety, production security, or native NCP 1.0. | PROVEN | `docs/release/0.9.0/PROTECTION-MODEL.md`; `release/0.9.0/protection-model.json`; `tools/release/test_verify_protection_model.py`; `tools/release/verify-protection-model.py` |
| CL-RECEIPT-01 | Every actor-admitted decision emits a `COSE_Sign1` `DecisionReceiptV1` signed by the Gate application key; the receipt verifies independently and is bound to the Gate's enrolled subject (H-B02/H-H03). The public actor accepts candidates only through a private-field `BoundedIntentCandidate` that rejects envelopes over 16 KiB and routes over 256 bytes in O(1), before hashing, decision-id allocation, verification, state mutation, or evidence work. Such pre-admission ingress rejection is not a decision and emits no receipt. This bound does not authenticate caller or transport provenance. | PROVEN | `haldir-gate` `decision_receipt_is_signed_and_verifies_to_gate_identity`; bounded-candidate exact-boundary tests |
| CL-IDENTITY-01 | Signer identities are bound to their roles and subjects: mission-authority to the lease issuer (H-H01), the controller-intent signer's trust-store subject to the claimed controller id (H-H02), the Gate signer to the Gate id (H-H03). | PROVEN | `haldir-gate` `accept_lease_env`, `spoofed_controller_id_denies`, receipt test |
| CL-LIVE-ACTIVATION-01 | Under the off-by-default live feature, a public non-cloneable no-network kernel consumes the private declared-live startup capability and can only issue one initial `GateChallengeV1`. Its nonce comes from the same startup cryptographic-entropy fill as the boot/output identities (`OsEntropy` uses the OS CSPRNG), is not accepted from activation input, and is registered under a fixed 30-second Gate-monotonic lifetime. Gate signs the exact challenge over Gate/boot/realm/vehicle/session/output-epoch/key/policy/contract-version/NCP-compatibility bindings. Only the returned move-only `IssuedLiveGateChallenge` exposes activation; `DeclaredLiveGateKernel` has no lease-activation method. Activation accepts one bounded caller-supplied initial trusted state and signed lease, installs the state and source-replay position before consuming the one-shot challenge or durable lease term, verifies the signed lease and admission-bound controller, derives `HaldirKeys::intent(controller_id)`, and requires the lease nonce, exact signed route, and installed-state source key to match before challenge consumption, durable term commit, authorization-revision change, or `Active`. Success returns a move-only `LiveIntentRouteBoundGate`; every issuance or activation failure returns no runtime owner. This proves initial Gate-signed challenge construction/registration, local expiry, typestate ordering, state/lease source intersection, and route derivation—not authenticated state or lease delivery, challenge-envelope transport, refresh/revocation/preemption, session opening/authentication, credential or handle exclusivity, concrete Zenoh invocation, or transport provenance. | PROVEN | `haldir-gate` exact signed-challenge binding/signature/tamper/local-expiry tests; activation input-bound/signed-lease-negative/wrong-challenge/canonical-route/noncanonical-route/state/source/clock/profile/route-bound-to-fake-publisher-binding and pre-commit validator-retry tests; external public-API/`Send` test |
| CL-LIVE-INGRESS-BINDING-01 | Under `live-zenoh`, public non-cloneable `DeclaredLiveGateZenohService::bind` consumes the accepted-lease-derived route capability, one caller-opened `SecureZenohSession`, and validated `IngressLimits`; it accepts no controller ID, route, keys, publisher, or raw event override. Before subscriber declaration it re-derives the exact actor-realm/NCP-session/accepted-controller intent route and cross-checks the retained signed-lease binding, constructs the final-command publisher from the same supplied session wrapper, and binds its route locally. `IntentIngress` declares that exact route with `Locality::Remote`; its callback rejects a non-PUT, unexpected observed key, or oversized payload before bounded copying/queueing and retains only bounded-label counters. Success retains the session wrapper, internally declared ingress, publisher-owning lower service, and capacity-one slot. The lower raw-event Gate service and its transitions are crate-private. A synchronous consuming state-update transition preserves the aggregate handles, shutdown latch, and any privately pending intent on acceptance or ordinary rejection; it uses the same Gate clock and source replay boundary but trusts the embedding caller for provenance (`CL-STATE-INGRESS-01`). The consuming `process_next_or_state_or_shutdown` transition additionally polls one caller-owned cancellation-safe state receive beside shutdown and the owned intent ingress, with deterministic shutdown/state/intent poll priority. A privately retained retry remains owned by the aggregate unless the intent branch actually wins; state or shutdown cannot silently discard it. Consuming `process_next` receives only from the owned intent ingress, privately retains a journal-capacity or restart-clearance refusal for byte-identical retry before newer receive, fail-stops otherwise-unreachable input/key/output-capacity refusals as owned-I/O invariant violations, and returns the aggregate only on a safe continuation. A cloneable monotonic local request handle can latch a safe-boundary shutdown request. The request is cooperative: legacy `process_next` ignores it, every clone is an irreversible local stop capability that production wiring must restrict, and successful latching acknowledges neither aggregate return nor cleanup because the owner/future may concurrently drop. Consuming shutdown-aware transitions observe it before work; a later request wakes an idle receive. An already-observable request is prioritized, but request and receive selection are not one atomic wall-clock action; if a concurrent event is selected first, the Gate decision/publication lifecycle completes to its ordinary transition without request-driven cancellation and the request remains latched for the returned owner. Ingress closure, Gate stop, bind/process cancellation, publication failure, state-update clock regression, or a terminal actor replay invariant returns no aggregate. Consuming `shutdown` attempts local ingress undeclare/quiescence/drain, then attempts session close while the service retains the Gate instance lock, and only then drops the service/publisher; it still attempts close when ingress cleanup fails and preserves combined local error classification. This adds no state transport/authentication, in-flight timeout, OS-signal runner, durable-journal finalization, session retirement, or graceful production-shutdown claim. Offline tests prove accepted-controller route source, one fake session-lineage token producing both handles, state-update acceptance/rejection/clock/invariant fail-stop, fake final-publisher-route mismatch, bind cancellation, safe continuation, pending shutdown-aware process cancellation/drop, journal-capacity retry ordering, invariant-stop classification, ingress closure, capability drops, safe-boundary shutdown request ordering and latching, state-before-intent poll priority, shutdown ordering/error classification, and public cloneable/`Send`/`Sync` handle plus consuming-future API shape; the fake seam is orchestration evidence only. This proves aggregate-local construction topology, bounded ingress, local state-update ownership, and same supplied session lineage—not successful concrete session/subscriber invocation within this claim, state-producer identity, mTLS peer identity, credential or global handle exclusivity, ACL delivery, remote declaration cleanup on cancellation/drop, reconnect, authenticated ongoing control/state ingress, mandatory service selection, complete mediation, delivery, acceptance, or application. Successful concrete bind/shutdown invocation is separately and narrowly bounded by `CL-LIVE-GATE-DEV-BIND-01`. Public lower actor, session, ingress, and independently constructible publisher APIs remain bypasses; the raw-event Gate service itself is not public. | PROVEN | `haldir-transport-zenoh` actual-key/size/queue/counter/locality/callback-barrier/API tests; `haldir-state` source-replay tests; `haldir-gate` accepted-controller/state-update/fake-common-lineage/final-publisher-route-mismatch/bind-cancellation/continue/process-cancellation/journal-capacity-retry/owned-I/O-invariant/closed-ingress/shutdown-order/error-matrix/drop tests, exact aggregate tests `declared_live_state_ingress_destroys_owner_on_actor_invariant_fault`, `declared_live_zenoh_shutdown_request_precedes_owned_ingress_receive`, `declared_live_zenoh_shutdown_waits_for_in_flight_publication_then_latches`, `declared_live_zenoh_private_retry_precedes_new_receive_and_shutdown_is_ordered`, and `declared_live_zenoh_pending_shutdown_aware_process_drop_destroys_every_owner`, helper tests in `shutdown_race_tests`, plus external API test `public_live_zenoh_aggregate_surface_is_root_exported_consuming_and_send`; all-feature compilation and doctests of concrete bind/update/process/process-or-shutdown/process-or-state-or-shutdown/shutdown transitions and the private lower-service boundary |
| CL-ALLOW-HONEST-01 | An ALLOW receipt is stamped `AllowPrepared` with stage `OutputPrepared`, never `AllowPublished`: the Gate authorized and prepared exact output bytes and does not claim a downstream delivery it did not observe. | PROVEN | `haldir-gate` `decision_receipt_is_signed_and_verifies_to_gate_identity` (asserts `AllowPrepared`/`OutputPrepared`, not `AllowPublished`) |
| CL-PUBLICATION-STATE-01 | In the cooperative in-process reference profile, first exact frame/plant-command access requires consuming a non-cloneable, actor-bound prepared token through the actor's single `Prepared -> PublishCalled` slot after rechecking monotonic time, active authority revision, ACL publication authority, unchanged causal state, the publication-margin deadline, remaining lease horizon, and checked active-horizon arithmetic. A crate-private split can retain an opaque validated token across a journal sync and repeats every safety check at a caller-supplied post-sync exposure instant; a post-sync failure exposes no bytes, fault-latches, and conservatively retains `PublishCalled`. Preparation/cancellation does not mutate published-command history. Caller-asserted modeled returned-ok attempts one transactional history commit: success resolves the slot to `Idle`; commit failure returns a typed no-retry error, fault-latches, and retains `PublishCalled`, so a command that may already be active cannot be replaced. An asserted error/timeout consumes the resolver token, fault-latches, retains the called slot, and blocks actor-issued replacement output. The direct actor/range caller remains trusted not to copy or resubmit exposed bytes. The split is exercised by the local-sync mechanism in `CL-GATE-LIFECYCLE-01`; an off-by-default public single-owner kernel selects the concrete strict-publisher method in code. The development smoke target binds and immediately shuts down without receiving an event, so neither it nor a live-session test invokes the concrete publisher. This is not evidence of transport invocation, delivery, receiver acceptance, or physical application. | PROVEN | `haldir-gate` publication-state/actor-binding/horizon/post-sync revalidation tests, `hold_remains_available_but_failed_success_commit_faults_and_retains_called`, `returned_clock_regression_charges_history_before_faulting`; `haldir-range` explicit `drive_plant` transition |
| CL-GATE-LIFECYCLE-01 | A crate-private consuming publication coordinator owns the fused `JournalBoundRunningGate` and one monotonic clock through every typestate transition; the public bound wrapper exposes no mutable actor or journal seam. Before actor decision/output-sequence mutation it requires a non-cloneable bounded-pool permit and reserves three maximum-sized logical journal units. Reservation refusal returns ownership without actor, journal, decision, or sequence mutation while retaining the sampled clock high-water. It verified-appends and locally `sync_data`-confirms the exact signed receipt, validates the opaque prepared token, signs and sync-confirms linked `PublishCalled`, samples time again, and only then commits actor exposure. Under `live-zenoh`, validated `DeclaredLiveZenoh` plus exact-NCP startup privately mints the move-only live capability; copied or forged reports and modeled-actor paths cannot construct it. Only live Called exposes one consuming `publish_once(FinalCommandPublisher)`, which rejects a mismatched realm/session route before frame access or invocation. A local publisher `Ok` is linked to synced ReturnedOk but terminates as application-unobserved and commits no action-history interval because NCP v0.8 begins TTL at unobserved plant-local arrival and the transport supplies neither bounded transit time nor authenticated application acknowledgement. Publisher error, terminal-boundary failure, cancellation, or unwind likewise returns no ready runtime or publisher. The lower raw-event service and its transitions are crate-private; the public aggregate accepts no raw event or publisher override (`CL-LIVE-INGRESS-BINDING-01`). Cold drop before first poll creates no Called; drop or unwind after Called destroys the owner and recovery records linked Unknown rather than inventing ReturnedError. Definite pre-terminal failure reopens Called as Unknown; committed-side ambiguity after an actual terminal append/sync reopens as that exact terminal state. Recovered Called-or-later history blocks new decisions pending external clearance; time is diagnostic only. Concrete public signatures compile and are `Send`, but no lifecycle test opens a live Zenoh session, declares the concrete subscriber, or invokes the concrete publisher. Runtime declaration and initial inputs remain cooperatively selected and unauthenticated; no runnable production service, authenticated ongoing controls, credential custody, supervision, crash/power-loss/live-session campaign, or complete mediation is established. Public lower actor/frame/transport APIs still permit bypass or resubmission, and there is no transport-principal, delivery, acceptance, application, Crebain/Engram, or physical-actuation claim. | PROVEN | `haldir-gate` live-capability startup/reference/forged-report/wire-cross-check, activation, internal lower-service route/input/capacity/result/drop/fault, fake aggregate binding/retry/closure/shutdown/drop/safe-boundary-request, tested-`Send`, lifecycle result/reopen/cancellation/timeout/panic/terminal-fault/restart-clearance/post-sync/journal-capacity/cached-decision/bounded-pool/clock-regression tests, including terminal local-Ok ambiguity and `publisher_ok_history_commit_failure_is_journaled_and_restart_blocked`; external compile-fail privacy and all-feature API compilation; `haldir-evidence` called-or-later recovery and reserved-append ambiguity tests |
| CL-PUBLICATION-EVIDENCE-PRIMITIVE-01 | The additive `PublicationStageEventV1` contract canonically binds one Gate decision, decision/producer boot IDs, vehicle, session, output position/frame digest/effective validity, exact prepared-receipt envelope digest, exact predecessor envelope digest, stage, and producer-local monotonic time. Contract validation distinguishes normal same-boot events from a distinct-boot `UnknownAfterPublish`; random boot IDs themselves do not prove later/current provenance. A single-Gate, retained-state-bounded pure reducer accepts only the checked publication tuple of an output-bearing `AllowPrepared` receipt followed by `Prepared -> PublishCalled -> ReturnedOk|ReturnedError|UnknownAfterPublish`, and rejects gaps/duplicates/terminal successors/identity drift/broken links/producer-boot-wide time regression. Its recovery planning helper rejects a boot ID already seen when constructing a batch and creates but does not apply one linked Unknown payload per distinct-boot dangling call; generic replay does not authenticate recovery freshness because one recovery boot may close multiple tails. `PublishCalled` is a write-ahead ambiguity boundary, not proof that transport began. This proves canonical payload/reduction primitives only: no COSE/journal Gate-role signer or receipt/envelope correspondence/size verifier, authenticated current-boot context, ordered manager replay, capacity reservation, actor/startup selection, append, crash recovery, transport call, or delivery. | PROVEN | `haldir-contracts::publication` canonical/golden/negative tests; `haldir-evidence::publication` reducer/recovery-plan tests |
| CL-ERROR-01 | Internal faults detected inside `decide_intent` before preparation (fault latch, monotonic-clock regression, TOCTOU revision change, NCP build/validate failure, or malformed action history required by velocity evaluation) yield an ERROR outcome with no output, distinct from an authorization DENY (H-H10). The Gate latches the stable detailed invariant code while the signed external receipt uses `ErrorInternalFault`. Hold evaluation deliberately remains history-independent so a stop command is not blocked by malformed motion-duty history. Later publication-transition failures are separately bounded by `CL-PUBLICATION-STATE-01`; they do not rewrite the already-signed `AllowPrepared` receipt. | PROVEN | `haldir-gate` `clock_regression_latches_and_errors`, `velocity_history_failure_latches_error_and_prepares_no_output`, `hold_remains_available_but_failed_success_commit_faults_and_retains_called` |
| CL-LEASE-USAGE-01 | The Gate enforces per-lease total-intent and fixed-point rate limits. Every fresh authenticated correctly scoped position reaching usage accounting consumes one finite total-budget unit even when rate-denied, so denied floods cannot avoid the lease total. Refill is from monotonic time only, carries exact sub-token remainder across calls so adversarial sub-quantum cadence cannot erase credit, discards excess elapsed credit at a full bucket, and never credits a clock regression (H-B07). | PROVEN | `haldir-gate` `lease_usage_tests`, including rate-denial total charging and denied sub-quantum calls |
| CL-LEASE-TRANSITION-01 | Lease acceptance is permitted only from `SESSION_BOUND`. An actor with unexpired active authority rejects replacement before monotonic-clock, anti-rollback-term, challenge, lease, revision, replay, or process-state mutation. At the exact active-lease expiry boundary, Gate may first materialize the time-driven transition to `SESSION_BOUND` and then accept a separately valid, higher-term successor using an already-pending challenge. | PROVEN | `haldir-gate` `active_actor_rejects_replacement_before_clock_term_or_challenge_mutation` and `exactly_expired_actor_can_accept_a_prestaged_successor_lease` |
| CL-PHASE-01 | A lease authorizes exactly one mission phase; an intent evaluated against a Gate-owned trusted state in a different phase is denied before policy runs (H-P04). | PROVEN | `haldir-gate` `mission_phase_mismatch_denies` |
| CL-STATE-DIGEST-01 | The canonical trusted-state digest commits every policy-relevant field; changing any single field changes the digest (H-B06). | PROVEN | `haldir-core` `digest_coverage` |
| CL-STATE-INGRESS-01 | Trusted-state ingress validates vehicle/session/source-session binding, validity, nonnegative uncertainty, `source receive <= capture <= Gate-observed monotonic time`, and source-sequence representability in the implemented NCP v0.8 JSON namespace; while active it rejects source keys outside the lease before they consume replay capacity. Capture time strictly advances. Boot-local replay tracks positions per source key under one global retained-epoch bound: a first epoch may attach at any nonzero sequence, active-epoch sequence strictly advances, a new opaque epoch retires the prior epoch, tombstones are never evicted, and capacity exhaustion fails closed. The lower actor API trusts its caller to supply the observation instant; the live services sample their retained Gate clock. Classification and commit occur under the same exclusive actor owner; any otherwise-impossible postvalidation disagreement fault-latches the actor instead of returning a reusable partial state. Consuming state-update transitions return the sole owner after normal rejection without replacing trusted state or mutating source replay, while clock regression or a terminal actor invariant returns no owner. The public aggregate can poll one caller-owned, cancellation-safe state receive beside its internally owned intent ingress and shutdown latch, so a quiet intent stream does not structurally block state refresh. This proves local validation/replay/ownership ordering and a usable embedding seam, not state truth, producer authentication, a state transport, runner, or end-to-end age. | PROVEN | `haldir-state::source_replay`; `haldir-gate` state-ingress, source-replay classify/commit invariant, NCP representability, and single-owner activity-race implementation plus focused boundary tests, including `trusted_state_replay_classify_commit_disagreement_fault_latches` and `declared_live_state_ingress_destroys_owner_on_actor_invariant_fault`; external live-service API shape; `haldir-policy-native` uncertainty boundaries; `haldir-range` stale-state scenarios |
| CL-POLICY-INTEGRITY-01 | The native evaluator fails closed on a semantically invalid policy, using allocation-free identifier validation. Integrated Gate startup additionally requires bounded, unique canonical phase/action and plant-mode/action sets; positive velocity/update and coherent validity/duty parameters; nonnegative safety distances; a usable geofence; enough local lease duration for the configured publication margin and minimum validity; a present schema-v2 motion envelope with an exact bounded local-NED frame identifier plus positive acceleration, slew, and continuous-motion bounds; and exact equality between the supplied policy identity and a canonical digest covering every executable policy field. Every action requires an exact digest-bound rule for the trusted plant mode; an unlisted mode denies, and rules are action-specific so permitting Hold cannot also permit velocity. Velocity authorization additionally requires the trusted-state/NCP frame identifier to equal that digest-bound policy value before Gate may copy it into an output frame. Set ordering is canonicalized because it cannot affect evaluation after duplicate rejection. Immutable digest schema v1 remains available only to identify legacy snapshots and retains its original preimage/digest vector; the evaluator and Gate reject it for execution. Immutable schema v2 retains the v1 semantics, adds the exact local-NED frame, local acceleration/slew/continuous-motion/Hold-dwell envelope, and plant-mode/action rules, uses explicit stable action tags, and has its own fixed preimage/digest vector. An executable-field, action-semantic, tag, or encoding change requires a distinct schema and encoder rather than altering either frozen schema. The Gate retains an immutable validated-v2 policy capability and its cached identity, so intent evaluation does not repeat whole-policy validation or digest construction. By itself this binds an already-loaded typed policy only to its local identity; package-bound startup additionally matches that identity to the strict Gate configuration and a revision-scoped `POLICY_AUTHORITY` approval verified under retained bootstrap trust. Neither path independently attests plant capability, authenticates the trusted-state producer, or deserializes the policy from artifact bytes. | PROVEN | `haldir-policy-native` v1/v2 policy validation/digest coverage/order/schema/golden/fail-closed/validated-path and action-specific plant-mode tests; `haldir-policy-native` `velocity_requires_the_digest_bound_exact_local_ned_frame`; `haldir-deployment` independent policy-approval tests; `haldir-gate` policy-binding, deployment-configuration mismatch-before-effects, and trusted-state-frame denial tests |
| CL-SLEW-01 | The slew bound scales with the actual monotonic time elapsed since the last published command (floored, capped at the nominal update); command bursts faster than nominal are not granted a full nominal step (H-P01). | PROVEN | `haldir-policy-native` `slew_bound_tracks_actual_elapsed_time` |
| CL-DUTY-01 | Within one retained actor history, locally reported returned-ok non-Hold command horizons form a private, validated, canonical half-open union bound to the validated policy's exact nonzero rolling window and a hard capacity. Window overlap is clipped and summed exactly in nanoseconds; widened comparison allows equality and denies one nanosecond over. At capacity, closest-gap compression deliberately over-approximates rather than dropping charge, and a 512-case reference property covers compression plus eviction. The prospective requested/NCP horizon is conservative rather than exact final validity; because effective validity subtracts the same maximum publication margin enforced at call exposure, it covers maximum call delay plus the published horizon. Invalid history required for velocity is a typed internal ERROR and fault latch, never `DenyDutyLimit`. Hold evaluation is the explicit safe-action exception, but a failed returned-ok Hold history commit fault-latches and retains Called. This is authorization accounting over locally reported command horizons—not proof of downstream delivery, acceptance, application, or physical motion—and it does not claim durable prior-boot history reconstruction; recovered Called-or-later traces already require explicit clearance. | PROVEN | ADR-0008; `haldir-core` duration boundary/conversion tests; `haldir-policy-native` exact-cap/+1ns, fractional-union, half-open clipping, constructor/window, structural/transactional/high-water, Hold/error-chain tests and 512-case widened-reference plus compression/eviction properties; `haldir-gate` history-binding/evaluation-error/Hold-commit/return-regression/error-chain tests, `publisher_ok_history_commit_failure_is_journaled_and_restart_blocked`, and configured call-deadline boundary |
| CL-FIXEDPOINT-01 | The native policy uses only integer/fixed-point arithmetic with widened comparisons; no floating point participates in authorization. Its prospective geofence projects from exact trusted-state capture time through the whole candidate horizon over an outward-rounded per-axis software interval containing requested velocity and measured velocity ± velocity uncertainty, then adds position uncertainty, configured tracking error, and policy margin. Accepted state age is part of that projection as well as a freshness/validity input. The implementation does not authenticate or bound physical acceleration, braking, overshoot, disturbance, localization error outside supplied uncertainty, or actuator behavior, so this proves deterministic software-envelope evaluation—not a physical reachable-set, containment, dynamics, or stopping-distance property. | PROVEN | `haldir-policy-native` `decide.rs`; state-age, sub-millisecond, geofence, measured-acceleration, uncertainty, overflow, and boundary tests |
| CL-DECISION-ID-01 | Decision ids are the domain-separated 128-bit digest of the full Gate boot id and a checked counter; exhaustion latches a fault and emits one reserved terminal receipt which is then replayed byte-for-byte rather than minting distinct decisions with a reused id (H-H06). | PROVEN | `haldir-gate` `decision_id_tests`, `decision_id_exhaustion_latches_error` |
| CL-FAULT-STATE-01 | Every actor fault latch also moves the public process state to terminal `FAULT_LATCHED`; the enforcement latch and reported lifecycle cannot disagree. Decision-id exhaustion emits and caches one terminal error receipt. Output-sequence exhaustion is likewise a permanent namespace error, never a transient overload denial, and exposes no prepared frame. | PROVEN | `haldir-gate` `clock_regression_latches_and_errors`, `decision_id_exhaustion_latches_error`, `output_sequence_exhaustion_is_a_terminal_namespace_fault` |
| CL-CONFIG-01 | In-process Gate construction is fallible and rejects a zero lease cap, an unknown/revoked/wrong-role/development/wrong-subject/wrong-public-key Gate receipt signer, or a future NCP publication lease bound to another session/output epoch. It retains the validated executable policy, constructs action history once from that policy's exact nonzero duty window and exported hard capacity, and propagates construction, startup-transition, boot-counter, package-binding, and durable failures. Public configuration/startup errors are non-exhaustive standard errors with stable codes; nested configuration, actor, deployment-binding, semantic anti-rollback, and durable-storage errors remain available through `Error::source`. Package-bound startup consumes a strict signed `GATE_CONFIGURATION` contract, requires independently approved trust/admission/revocation/policy snapshot identities, and exact-matches identities derived from the complete live configuration before effects. Direct/development construction remains caller-authoritative; secure-transport and credential artifacts remain byte-only. | PROVEN | `haldir-gate` Gate configuration/error-chain/package-startup and every-configuration-field mismatch-before-effects tests, `gate_binds_history_capacity_and_window_to_validated_policy`; `haldir-deployment` Gate-configuration and authority-approval composition tests; `haldir-state` durable-wrapper standard-error test; `VehicleActor::new_ephemeral` |
| CL-DURABLE-PRIMITIVE-01 | The storage primitive authenticates store/Gate/generation/predecessor/payload with HMAC-SHA256, requires explicit provisioning, reconciles only an authenticated exactly-next snapshot with a configured compare-and-set generation anchor, and rejects missing anchors, rewind, fork, gaps, wrong keys/bindings, tampering, and overwrite provisioning. A replacement or anchor error poisons further commits until controlled reopen because the failed operation may have taken effect. The Unix file backend performs bounded same-directory create/write/file-sync/rename/directory-sync, rejects symlinks, and binds every leaf operation to one verified parent-directory descriptor so replacing its pathname mid-operation cannot redirect I/O; the anti-rollback wrapper changes live state only after commit, derives boot IDs from Gate ID + checked counter + injected entropy, persists the last ID, and rejects repeats. Lease terms use an injective, versioned canonical `(logical issuer, vehicle)` namespace; acceptance also reads the old delimiter-based namespace as a conservative migration floor. Durable state is bounded by map cardinality, scope length, and total canonical bytes; every successful public update is reopenable within those limits, while capacity failure is atomic in live state, replacement snapshots, and anchor state and is exposed to Gate lease acceptance only as store unavailability. Durable format v3 explicitly distinguishes pristine-unbound, package-bound, and migration-required state. Valid canonical v1/v2 state at the exact byte boundary retains its original wire format and semantic high-waters until a successful mutation can migrate it within the v3 bound; unrelated successful v3 writes preserve the migration floor, pre-v3 readers reject the changed shape, and a migration without sufficient v3 headroom fails without mutation. Plain use permanently marks an unbound store migration-required for package binding. `begin_deployment_boot` atomically commits one store-global package revision/payload-digest pair for the store's authenticated Gate binding plus the next boot: same/same restarts, higher revision advances, lower revision or same-revision/different-digest fails before mutation, and a bound store rejects plain boot. Storage failure exposes neither candidate; an uncertain anchor advance recovers both together. Distinct non-cloneable plain-booted and package-booted capabilities are returned only after commit, and both reject a Gate outside the authenticated store binding. Gate has distinct recovered constructors for both capabilities; the package-bound startup passes the verifier-derived revision/digest directly into the atomic operation and retains the validated package. A store-unavailable lease-term commit grants no authority and latches a fault. This proves unit-tested mechanisms, not an external anchor deployment, child-process crash recovery, power-loss durability, protected bootstrap/package acquisition, cross-store high-water transfer, or Windows support. | PROVEN | `haldir-durable` RFC 4231/envelope/recovery/ambiguous-commit/file tests; `haldir-state` v1/v2/v3 canonical migration, exact-boundary legacy preservation, migration-headroom, bounded-cardinality/scope/bytes, successful-reopen, capacity-atomic live/snapshot/anchor, package first-bind/idempotence/upgrade/rollback/equivocation/plain-downgrade, atomic failure/recovery, scope collision/injectivity, boot-vector/binding/repeat/failure-order/reopen tests; `haldir-gate` plain/package recovered-startup, same-revision equivocation, and failure-latch tests |
| CL-DEPLOYMENT-PRIMITIVE-01 | `DeploymentPackageV1` is a strict canonical v1.0 contract binding deployment authority/id/nonzero revision, Gate/realm/vehicle, separately matchable class/runtime/NCP-wire selections, distinct nonzero state/journal IDs, and exactly one canonical ordered reference for every closed required artifact role. Each reference binds a unique logical ID, nonzero size, and deployment-artifact-domain digest. `haldir-deployment` verifies the exact COSE payload under separately supplied bootstrap trust/revocations and grouped identity, execution-profile, and authority-approval policies; it requires `DeploymentAuthority`, enforces assurance key class and signer subject, and retains the exact bounded bootstrap trust/revocation snapshots inside the verified typestate. Its consuming resolver preflights per-artifact/total limits, checks the exact role/logical-ID/length/digest set, retains the same owned bytes with no path/reopen API, and redacts them from `Debug`. Successive consuming stages validate the exact signed `NcpCompatibility` bytes, strictly decode and package-cross-bind `GateConfigurationArtifactV1`, then verify the `TrustManifest`, `AdmissionSnapshot`, `RevocationSnapshot`, and `PolicySnapshot` artifacts as independent `AuthoritySnapshotApprovalV1` COSE envelopes. Required signing roles are selected from the closed artifact role before payload decode; every approval exact-matches its kind, trusted signer subject, externally expected issuer, deployment id/revision, Gate/realm/vehicle, and the corresponding strict configuration digest. Approval verification uses the same retained bootstrap trust/revocations as package verification, so a later caller cannot substitute another root. The final `AuthorityValidatedDeploymentPackage` is the only package typestate accepted by package-bound Gate startup. After the signed runtime trust digest exact-matches, startup requires retained bootstrap and runtime trust to be disjoint by both `kid` and public-key material, rejecting even identical overlap so independently governed revocation snapshots cannot create ambiguous lifecycle or resurrection. Startup derives and exact-matches all approved live snapshot identities plus session/publication/local-cap/Gate-signer/runtime/store/assurance selections before entropy or backend access, atomically commits the verifier-derived package revision/payload digest with the fresh boot, retains the proof, and exact-matches the package journal ID before binding format-v2 evidence. The unbound startup API rejects `AssuranceExternal`. On Linux/macOS the optional fused source provides bounded no-reopen capture relative to one caller-supplied directory descriptor with the documented flat-name, topology, size, mutation, and digest checks. This proves the implemented cryptographic and local composition only: bootstrap policy/root provenance and freshness remain caller-supplied; approval artifacts authenticate semantic snapshot identities but do not deserialize or acquire the runtime snapshots; seven roles remain byte-only; root ownership/mode/writer exclusion, atomic or wall-clock-bounded I/O, protected credentials, mandatory journal/path, running-image correspondence, mandatory production selection, an authenticated runner, and verification-before external secret/network acquisition remain absent. | PROVEN | `haldir-deployment` strict schema/hostile-parser/signature/scope/profile, exact owned-artifact, signed-NCP, strict Gate-configuration, retained-bootstrap/no-second-root, four-role approval/cross-binding/replay/key-class, and Linux/macOS directory-capability tests; `haldir-crypto` within- and cross-store trust/role and snapshot-digest tests; `haldir-admission` snapshot-digest tests; `haldir-gate` package-bound success, every-static-configuration-field mismatch-before-effects, bootstrap/runtime trust-conflict, unbound-assurance, external-assurance, retained-proof, journal-binding, and same-revision-equivocation tests; `haldir-contracts::deployment`; `haldir-state` package-ratchet tests |
| CL-LOCAL-ANCHOR-DEV-01 | The fixed-format local file generation anchor is bound to one `StoreId`, checksums corruption, enforces exactly-next compare-and-set, and reports `LocalRewritable`. It supports post-provisioning Unix process-crash reconciliation only; the checksum is not authentication, its CAS assumes the retained exclusive writer lock, and it cannot satisfy external anti-rewind, power-loss, hostile-directory, or multiwriter requirements. | PROVEN | `haldir-durable::local_anchor` persistence, binding, conflict/gap, corruption, truncation, and authenticated-snapshot integration tests |
| CL-DURABLE-STARTUP-DEV-01 | The Gate template-startup library validates static signer/cap/current ACL-only configuration, store binding, size, exact anchor-protection profile, and an explicit `GateRuntimeProfile`. `DeclaredLiveZenoh` requires both the compiled `live-zenoh` feature and `ExactNcpV0_8Json`; feature-disabled or modeled declarations fail before any startup-owned `GenerationAnchor`/`SnapshotStorage` trait call, entropy request, lock acquisition, or local-directory access. `InProcessReference` retains the existing reference paths. Successful startup retains the validated runtime declaration in `StartupReport` as observable process-local data and, only for validated declared-live startup, internally derives and privately retains a distinct non-cloneable capability that can be consumed by the live coordinator constructor. In-process builds obtain 48 startup entropy bytes (32 boot and 16 output epoch); `live-zenoh` builds obtain 80 bytes by appending a 32-byte initial challenge nonce. Each build performs one complete fill before durable open/provision, holds a cooperative exclusive instance lock, requires explicit provision/open mode, surfaces pending-anchor recovery and the boot commit, commits a fresh Gate boot before actor construction, and generates its output epoch and, when compiled live, challenge nonce fresh when the injected `EntropySource` contract is satisfied. Local convenience startup is restricted to `LocalRewritable`; assurance startup accepts only an injected `ExternalNonRewindable` implementation. The runtime declaration remains caller-supplied and unauthenticated on the explicit development/unbound path; package-bound startup instead commits and exact-matches it through the signed Gate configuration. The internally derived live capability is process-local rather than an external attestation. Public `GateConfig`/direct `VehicleActor` construction bypasses template startup and cannot mint that capability. This proves per-invocation library ordering, development-local wiring, and the process-local construction provenance in `CL-GATE-LIFECYCLE-01`, not mandatory service selection, non-cooperating-writer exclusion, a deployed external anchor, live-session creation, transport invocation, child-process crash behavior, or power-loss behavior. | PROVEN | `haldir-gate::startup` validation/entropy/runtime-profile/default-feature/live-feature/no-startup-call/report/private-capability/live-coordinator-composition, signed-configuration mismatch-before-effects, explicit provision/open, lock exclusion, repeated-ID, pending-recovery, local-file, and `Send` tests |
| CL-NCP-MODEL-01 | The P0 modeled NCP adapter rejects zero effective validity, validity greater than the signed action request, and source/output sequences above NCP's JSON-safe integer maximum. Gate rejects an unrepresentable source at state ingress and treats that same maximum as its terminal output-allocation boundary, so the next output is `ErrorNamespaceExhausted` rather than a generic adapter fault. Its exact prepared frame cannot be externally mutated; internal frame/bytes/digest/transformation drift, including coherently rebuilt zero validity, is rejected. This is model hardening, not real NCP interoperability. | PROVEN | `haldir-ncp08` validity/sequence-boundary and validator-tampering tests; `haldir-gate` source-ingress and output-namespace boundary tests |
| CL-NCP-COMPATIBILITY-01 | `haldir-ncp08` defines a strict canonical v1.0 `haldir.ncp_compatibility` artifact under a dedicated 512-byte limit. Canonical decoding rejects wrong kind/version, missing/unknown/duplicate/reordered fields, non-shortest/indefinite forms, trailing bytes, and parser-limit excess. Acceptance exact-matches the implemented frozen command-frame subset—NCP tag/commit/wire version/contract hash/proto digest, command-schema digest, command-vector digest, enabled increment, capability profile, and compiled adapter package version—to `NCP_V0_8_0`, and only then returns a private-field validation proof. Its compatibility identifier binds every compiled field through a domain-labelled canonical array, and modeled/default plus exact-feature adapters expose the same record. The standalone validator proves only explicitly supplied ordinary bytes; the consuming deployment stage additionally binds its proof to the exact resolved bytes of the signed `NcpCompatibility` role, and package-bound Gate startup consumes and retains that stage before its own effects. These paths do not prove aggregate schema/conformance-set identity, adapter source/build or running-binary provenance, artifact-root authenticity, credential custody, or full production-startup ordering. | PROVEN | `haldir-ncp08::compatibility` golden/round-trip/every-field/hostile-shape/resource-bound/arbitrary-byte/reason-code tests; `haldir-ncp08::selection` modeled/exact record agreement; `haldir-deployment` signed-role composition/incompatible-role tests; `haldir-gate` package-bound startup tests; `tools/verify-pins.py` command-subset pin/file checks |
| CL-NCP-REAL-01 | With the off-by-default `real-ncp` feature, Haldir constructs an upstream NCP v0.8.0 `CommandFrame` from Gate-owned semantics, validates before and after exact JSON serialization, and profile-rebuilds immutable frames for self-consistency before plant-model use. Reverse velocity conversion rejects non-finite, out-of-range, and finite non-image values instead of mapping them to zero or saturating; a plant command retains the successfully recovered fixed-point action and nonzero validity. The exact JSON bytes bind the NCP-carried source epoch/sequence but cannot bind Haldir's `source_key`, for which NCP v0.8 has no field. Gate/source integer nanosecond times are deterministically projected to NCP binary64 seconds; this is non-injective over the full `u64` domain and has a tested 2,048 ns full-domain round-trip bound, so the byte digest does not uniquely commit the original nanosecond values. Frozen-corpus, differential, intentional source-key/time-projection, conversion-boundary, and tamper tests run at the immutable NCP commit. A closed adapter selector has no caller-defined implementation seam or default constructor; Gate template startup requires explicit adapter and runtime-profile values, preserves exact selection through durable startup, and an exact-selected actor is tested through the Called boundary with upstream JSON and the receipt-bound digest. `GateRuntimeProfile::DeclaredLiveZenoh` additionally requires the compiled `live-zenoh` feature and rejects `ModeledP0` before startup-owned backend calls, entropy, locks, or directories; `InProcessReference` remains available for modeled P0. Cargo feature unification can compile the exact constructor or live code but cannot change either stored selection. A successful `StartupReport` retains only the observable declaration; a separate private startup capability gates the live coordinator typestate, which also cross-checks exact wire selection. This proves selectable exact construction, a cooperative guard over the listed startup-owned calls/accesses, and process-local coordinator provenance under `CL-GATE-LIFECYCLE-01`, not authenticated or durable profile binding, runnable-service selection, live-session creation, concrete publisher invocation, Zenoh delivery, ACL exclusivity, or plant application; direct `GateConfig`/`VehicleActor` construction bypasses template startup and remains outside the coordinator chain. | PROVEN | `haldir-ncp08::conversion`, `real`, and `selection` tests; `haldir-reference-plant` exact-command construction tests; `haldir-gate::startup` runtime-profile/default-feature/live-feature/no-startup-call/report/private-capability/live-coordinator-composition and exact-selection tests; `haldir-gate::e2e::explicitly_selected_exact_adapter_reaches_the_called_boundary_as_ncp_json`; frozen corpus hashes enforced by `tools/verify-pins.py`; exact git source in `Cargo.lock` |
| CL-TRANSPORT-BOUNDARY-01 | Standard NCP command/sensor routes are built by the pinned NCP v0.8 key implementation; Haldir extension routes are bounded and exact. On Linux/macOS, the off-by-default Zenoh 1.9 boundary captures one explicit configuration only from a no-follow, nonblocking regular-file descriptor under a 256-KiB cap, then parses the captured JSON5 directly so Zenoh cannot load external plugin configuration before Haldir validation. Other targets reject this file-loading path. It rejects non-client, non-TLS, listener/discovery-enabled, identity-less, hostname-unverified, expiration-unchecked, auxiliary-surface-enabled, or unbounded receive/connect configurations; intent ingress reserves bounded queue capacity before copying and carries the observed key without inventing a certificate principal; final publication accepts only upstream-validated NCP JSON for its bound session/base command route and excludes session-local delivery. The deterministic secure-reference package pins the router image and post-load launch overrides, emits a closed TLS/mTLS/default-deny ACL whose single-flow grants distinguish publisher ingress from receiver egress, restricts wildcard authority to pinned NCP's RPC queryable declaration only, and statically proves Gate is the sole final-command `put` ingress principal. The captured config is not an authenticated deployment artifact, and ordinary filesystem I/O has no wall-clock bound. This row proves code/configuration shape; the separately bounded receiver-observed subset is `CL-LIVE-TRANSPORT-01`. Neither establishes exclusive custom-CA server trust, Gate-service selection, protected credential custody, Crebain receipt/application, or complete mediation. | PROVEN | `haldir-transport-zenoh` default/live-feature route, bounded regular-file/no-follow config, strict profile, ingress bounds, JSON/session, locality, and API tests; `tools/test_secure_zenoh.py`; `tools/verify-secure-zenoh.py`; `tools/verify-pins.py`; `cargo deny --all-features check` |
| CL-EVIDENCE-01 | Every decision (ALLOW or DENY) attempts to append its signed receipt to a bounded, digest-chained in-process spool; a full spool drops only the export copy and can never change a decision. Local verification detects retained-record or link-metadata corruption. Coordinated prefix truncation requires an externally protected count/head checkpoint through `verify_chain_against`; the spool cannot witness its own missing suffix. The chain is in-process — crash-durability is out of P0 (`CL-DURABLE-01`). | PROVEN | `haldir-gate` `decisions_are_journaled_to_the_evidence_chain`; `haldir-evidence` corruption/checkpoint/full-spool tests |
| CL-EVIDENCE-SEGMENT-PRIMITIVE-01 | The Unix journal primitive creates bounded one-writer evidence segments whose format-v2 headers bind Gate ID, a typed nonzero logical journal ID, boot ID, declared signer key ID/public key, positive segment sequence, previous segment-content digest, and creation time. Genesis and successor constructors reject nonzero genesis links, Gate or journal substitution, sequence gaps/rewinds, and wrong predecessor digests. Every opaque caller-validated record is length-framed and CRC32C-protected; the footer binds record count, record-chain head, signed header/record-content digest, and an Ed25519 signature. Recovery returns all complete records, truncates only a structurally matching insufficient final record/footer tail, and rejects short garbage, wrong partial versions, identity/signer substitution, bad complete-record CRC, header/footer tampering, bad signatures, symlinks, and bound overflow. This does not prove the opaque records are canonical/signed, KID/public-key trust/role resolution, directory-wide chain discovery/retention/locking, Gate selection/rotation/reserved-capacity policy, process-crash behavior, power-loss durability, or external availability. | PROVEN | `haldir-evidence::journal` CRC vector, v2 header bound, genesis/successor journal continuity, inspection, round-trip, strict-tail, complete-corruption, identity/signer, signature, symlink, and bound tests |
| CL-EVIDENCE-MANAGER-01 | The Unix evidence manager requires explicit new/existing mode and a typed logical journal ID, requests owner-only permissions when provisioning its journal directory and files, retains a checked exclusive directory lock, accepts only canonical contiguous segment filenames and chain links, rejects a Gate or journal-ID mismatch before prior-tail closure or successor creation, resolves every declared signer, and calls an injected candidate verifier whose contract requires deterministic side-effect-free validation. It retains only the current signer KID/public key and requires an exact short-lived private-key borrow before any append/finish operation that may footer-complete; a wrong borrow fails before record mutation. It closes an old active tail only with its exact historical signer, supports current-key rotation, atomically publishes new headers, rotates a pending record once, and quiesces at segment/byte caps without deletion. Domain-separated exact-record digests reject byte-identical duplicates and let recovery answer whether an ambiguously attempted record is present before retry. An opt-in API returns verifier-accepted opaque bytes grouped by authenticated segment in exact journal order only after the whole open succeeds, with separate recovered-record/exact-record-byte caps and no partial snapshot on error; legacy open remains non-capturing. Opaque manager-affine reservations conservatively retain one future segment slot and `260-byte maximum header + maximum record frame + footer` per unit; ordinary appends/rotations cannot consume those logical byte/segment/sequence quotas, confirmed reserved appends consume one unit, and foreign/reused tokens fail without releasing the issuer's quota. A fused recovery precommit may return a bounded current-boot record batch whose record bounds, duplicates, maximum-header quota, and successor layout are checked before prior-tail closure/current creation, then appended under a reservation before open returns. This proves unit-tested opaque journal orchestration, single-owner-compatible signer custody, bounded recovery output, and configured logical quota isolation—not validation or repair of permissions on an existing directory, protection from a trusted-parent violation, filesystem preallocation/free-space availability, COSE semantics, verifier purity enforcement, Gate actor selection, live lifecycle policy, OS fault-injection/child-process crash behavior, power-loss durability, retention, or external witnessing. | PROVEN | `haldir-evidence::manager` provisioned-directory permission, provision/open/incomplete-genesis, lock, Gate/journal mismatch before mutation, chain/fork/gap, borrowed-signer/record, cap/quiesce, rotation, pending-header, key-rotation, duplicate recovery, ordered bounded recovery-snapshot, maximum-header, reservation affinity/exact-bound/non-consumption, and precommit tests |
| CL-GATE-JOURNAL-REPLAY-01 | The assurance-only `GateJournalVerifier` statelessly exact-resolves each segment KID to an unrevoked `GateApplication` assurance key whose subject/public bytes bind the configured Gate, and accepts only same-KID Gate-signed `DecisionReceiptV1` or `PublicationStageEventV1` envelopes under an explicit whole-envelope bound. A one-parse protected-content-type dispatcher selects the matching fixed kind/v1/AAD and typed canonical decoder; generic `verify_and_decode<T>` also rejects context/type kind or major-version confusion. Receipts must match the current Gate outcome/reason/stage/output/time profile, and every record Gate/producer boot must match its supplied trust-bound segment identity. Consuming a manager-produced ordered recovery snapshot reverifies every record, includes empty segments in provenance, rejects duplicate receipts, boot resurrection and same-boot segment/record time regression, skips valid DENY/ERROR receipts for publication state, and returns opaque read-only fresh publication state plus recovered-boot/tail provenance only after the whole replay succeeds. Existing linked `UnknownAfterPublish` is thereby bound to a later contiguous journal boot group, not merely a different random ID. “Exactly once” means each captured record affects one newly constructed state at most once; reopening rebuilds fresh state. Direct record verification alone does not prove segment membership or chain order. This proves the offline trust adapter and authenticated ordered semantic replay only—not trust/revocation snapshot freshness or historical key validity intervals, cross-receipt output-sequence allocation invariants, other journal record kinds, current active-boot authority or new Unknown emission, capacity reservation, append coordination, post-replay live mutation, actor/startup/service selection, crash campaign, transport invocation, delivery, or receiver acceptance. | PROVEN | `haldir-crypto` context/type/version-confusion regressions; `haldir-evidence::gate_journal` closed dispatch, signer/Gate/boot/bound/receipt negatives, mixed replay, later-boot Unknown, boot/time chronology, and coarse manager-adapter tests |
| CL-GATE-JOURNAL-BINDING-01 | The staged Gate startup journal APIs derive the current Gate ID, freshly committed boot, assurance signer, and trust/revocation verifier from the actual non-forgeable `RunningGate`, not `StartupReport` or caller-supplied IDs. The journal ID is an explicit typed selection; when startup retained a signed package it must exact-match that package before directory access and is rechecked against the manager-created active identity. Directory recovery, bounded record capture, and publication replay are fused under one verifier configuration into a non-extractable manager/state aggregate. On restart, every replayed dangling `PublishCalled` is deterministically linked, signed as current-boot `UnknownAfterPublish`, trust-verified, semantic-previewed on a cloned reducer, and checked against both future recovery-capture bounds and conservative manager quota before prior-tail closure/current-segment creation; confirmed records are appended and only then installed into the returned state. Semantic replay/current-boot-freshness rejection never closes the old tail; for a nonempty Unknown batch, capture/logical-capacity rejection also cannot close it or burn a successor. An empty batch retains the manager's ordinary recovery/quiescence semantics. Earlier insufficient-tail truncation or pending-artifact removal remains possible; its action is reported on successful open and semantic-replay rejection, while another later recovery failure can return without an exact action report. Trace capacity is conservatively preflighted against the capture record bound, unusable genesis capacity fails before directory creation, fresh journal provision requires freshly provisioned durable Gate state, and restart requires journal open. Binding rejects no active segment, Gate/journal/boot/signer drift, current-boot resurrection, or an active sequence outside recovered-tail-plus-one through the bounded startup-record rotation span. The retained bound type exposes only read-only actor, manager-created/trust-resolved active identity, durable startup report, open-recovery report, recovered-publication state, and appended-Unknown count; it remains `Send` and drops the journal lock before the Gate instance lock. Forged journal boot options, same-boot recovery, restart-to-fresh-journal forks, dangling-call closure, semantic retries, capture exhaustion, and logical capacity no-burn failures are negative-tested. This proves an in-process staged startup ambiguity closure only: logical reservation is not filesystem preallocation, the selected journal path is not committed into durable Gate configuration, and direct `VehicleActor` construction/in-memory evidence remains available. The development examples deliberately split fresh state+journal provisioning from `OpenExisting` bind/shutdown, but no authenticated mandatory selection, child-process crash campaign, transport publication, delivery, or receiver acceptance follows. | PROVEN | `haldir-gate::startup` fresh/restart/report/capacity/forged-boot/signed-journal-mismatch-before-directory/current-boot-resurrection/fresh-journal-fork/dangling-Called binding and `Send` tests; `haldir-evidence::gate_journal` fused precommit replay/current-boot Unknown/capture/reservation/no-segment-burn and trace-bound tests; `haldir-evidence::manager` genesis-capacity/maximum-header/reservation tests; `haldir-gate` development example provisioning tests |
| CL-INTEROP-01 | The `COSE_Sign1`/Ed25519 envelope and deterministic-CBOR payload codec are decoded and verified by an independent, dependency-free second implementation over shared vectors, which also rejects tampering and mismatched kind/key. Vectors cover every wire major type and integer width; per-contract schema vectors are a future extension. | PROVEN | `tools/interop/verify_cose.py` over `tools/interop/vectors.json` (emitted by `haldir-crypto` `emit_interop_vectors`); CI `interop` job |
| CL-CI-01 | The Rust quality gate (build, clippy `-D warnings`, tests, docs, fmt) runs on a single pinned toolchain reproducibly (H-B01). | PROVEN | `.github/workflows/ci.yml` `build-test`; `rust-toolchain.toml` |
| CL-CI-02 | Every third-party GitHub Action and the exact TLA+ release asset are pinned immutably; automated drift checks reject mutable refs/URLs or a missing digest verification. | PROVEN | `tools/verify-ci-pins.py`; CI `supply-chain`; `.github/workflows/formal.yml`; `tools/pins.toml` |
| CL-FORMAL-01 | The finite TLA+ authority model checks type safety, retired-epoch exclusion, gap/reuse-free output allocation within the modeled epoch, consistent lease presence, exact active-lease binding to the current boot/session, and terminal-fault immutability. The current source is locally model-checked under the pinned TLA+ v1.7.4 jar; historical hosted runs bind only their older exact commits and do not establish hosted evidence for the current source. | PENDING | `formal/HaldirAuthority.tla`; `formal/HaldirAuthority.cfg`; exact jar digest in `tools/pins.toml`; a current exact-subject hosted formal result is required before promotion back to PROVEN |

## Deployment-boundary and later-profile claims

This section mixes broad claims that remain unproven or out of scope with
narrow, already-proven boundary facts that explain exactly what the existing
evidence does establish. The status column is authoritative; a `PROVEN` row
here does not promote any broader deployment claim.

In this ledger, an “independent” approval means only a separately verified
signature under a distinct required role and distinct enrolled public key. It
does not assert separate organizations, operators, administrators, or failure
domains unless a claim says so explicitly; no current claim does.

| Claim | Statement | Status | Evidence |
| --- | --- | --- | --- |
| CL-DEPLOYMENT-PACKAGE-01 | Every live-authority boot is selected by one strictly decoded, signed deployment package that binds the Gate/realm identity, exact NCP artifact and wire/contract pins, runtime profile, binary/config/trust/admission/revocation/policy digests, state and journal identities, router/credential identity, and monotonic package revision; verification precedes durable, entropy, secret, or network effects, unbound overrides are forbidden, and the accepted revision/digest cannot be downgraded across restart. | UNPROVEN | Package-bound Gate startup now consumes NCP and strict Gate-configuration stages, four separately verified, role-separated, public-key-distinct snapshot approvals under the same retained bootstrap trust/revocations, and the package-booted store stage. This proves neither separate organizations nor independent operators. It exact-matches every live authorization snapshot/session/publication/cap/signer identity, atomically ratchets the verifier-derived revision/digest, retains the proof, exact-matches its journal ID when a format-v2 evidence chain is bound, and closes the unbound `AssuranceExternal` path (`CL-DEPLOYMENT-PRIMITIVE-01`, `CL-NCP-COMPATIBILITY-01`, `CL-DURABLE-PRIMITIVE-01`). The full claim remains false because bootstrap policy/root selection, freshness, and protection are caller-supplied; approval artifacts authenticate digests but do not load the runtime snapshots; seven signed roles remain byte-only; journal binding/path are not mandatory and the running image is not bound; no protected secret loader or authenticated control-plane runner exists; development live startup may remain unbound; and no composition proves package verification preceded external acquisition, secret, or network effects. |
| CL-LIVE-CONTROL-PLANE-01 | A live Gate issues its own signed challenge and accepts authenticated, bounded ongoing state, lease, admission, revocation, session-refresh, handoff, preemption, and restart-clearance updates from explicitly authorized producers. | UNPROVEN | The initial Gate-signed challenge issuer and local one-shot activation boundary now exist (`CL-LIVE-ACTIVATION-01`). Initial state and lease delivery remain caller-supplied, however, and the bounded consuming ongoing state-update seam (`CL-STATE-INGRESS-01`) authenticates neither its producer nor delivery. There is no authenticated ongoing lease/admission/revocation/session/handoff/preemption/restart-clearance ingress or authenticated transport/session/plant/history clearance mechanism. |
| CL-LIVE-GATE-DEV-BIND-01 | A retained clean-source synthetic run against the pinned Zenoh 1.9 router/profile and fresh ephemeral Gate PKI shows that the explicitly development-only `live_gate_dev_bind_shutdown` target opens a separately provisioned disposable state/journal fixture, performs caller-local boot/epoch-bound activation, opens one strict Gate session, binds the concrete aggregate on the accepted controller route, and receives local success from immediate explicit shutdown while processing zero intents and publishing zero commands. This proves only those concrete calls and local returns in that run—not authenticated package/control provenance, credential custody, peer identity, exclusive custom-CA trust or handle ownership, remote undeclare/session retirement, journal finalization, external anti-rewind/crash durability, timeout/signal supervision, delivery, acceptance, application, reconnect, bypass closure, complete mediation, retry/endurance behavior, or production readiness. | PROVEN | `evidence/12-live-gate-dev-smoke` at source commit `3a75c039c3e73b999a74741b5633ee43a0a69e97`; `tools/verify-live-gate-dev-smoke.py`; `tools/test_live_gate_dev_smoke.py`; `tools/test_live_gate_dev_smoke_verifier.py`; `haldir-gate` development-example tests. |
| CL-DURABLE-01 | Anti-rollback high-water and evidence survive a crash via durable authenticated storage. | OUT OF SCOPE | Authenticated snapshot/anchor primitives, development-local startup, a bounded signed evidence manager, and the internal ordered lifecycle mechanism exist (`CL-DURABLE-PRIMITIVE-01`, `CL-DURABLE-STARTUP-DEV-01`, `CL-EVIDENCE-MANAGER-01`, `CL-GATE-LIFECYCLE-01`), but no service package selects a deployed external non-rewindable anchor or makes the journal mandatory for every decision, and no child-process crash campaign exists — see `docs/LIMITATIONS.md` |
| CL-LIVE-TRANSPORT-01 | In the pinned `haldir-secure-reference-v1` synthetic deployment, only Gate among all eight configured certificate principals had exact final-command PUTs observed by the allowed remote robot/observer sessions. Seven non-Gate final PUTs and a controller final subscription produced no callbacks; each controller's own intent reached the remote Gate while both cross-controller routes did not; a no-client-certificate TLS connection was rejected. The result uses twelve unique sessions, valid pinned-NCP JSON for final commands, quiescent bounded callback drains, and a two-second late-quarantine window. This proves only the exercised ephemeral-PKI command/intent ACL subset—not arbitrary certificate lifecycle/reconnect, exclusive custom-CA server trust, runnable Gate selection or credential custody, Crebain application, bypass closure, or complete mediation. | PROVEN | `evidence/11-secure-zenoh-live` at source commit `a10f41f7edd2309520652b87e4d4c5c1e4cbf3c8`; `tools/verify-live-secure-zenoh.py` |
| CL-BACKEND-01 | The admitted neural backend is equivalent to a reference. | OUT OF SCOPE | admission is digest-equality only; no neural runtime and no backend equivalence is claimed in P0 |
| CL-HARDWARE-01 | Behaviour is validated on flight hardware. | UNPROVEN | simulation-only reference plant; nothing here is hardware validated or airworthy |
| CL-TIMING-01 | The Gate meets a real-time deadline. | UNPROVEN | no performance campaign was run; P0 makes no timing claim and is not hard real-time |
| CL-PRODUCTION-01 | The system is production ready. | UNPROVEN | P0 is a research/reference core and is not production ready; it is not certified for any operational use |
| CL-FINAL-PUBLISHER-SESSION-01 | The concrete Zenoh final-command publisher is constructed with the complete expected NCP session pair. It rejects a route/session-id mismatch at construction and, before invoking Zenoh, requires the immutable exact-frame semantics and decoded upstream JSON to agree with both the expected session id and generation. Its `publish` method consumes the particular publisher handle, preventing reuse of that handle after a definite or ambiguous local return. This is local pre-publication validation and single-handle linearity; it proves neither exclusive session/credential custody nor remote delivery or freshness of the caller-supplied expected pair. | PROVEN | `haldir-transport-zenoh` wrong-id, wrong-generation, invalid-frame, successful exact-JSON publisher-boundary, and consuming API compilation; `haldir-gate` publisher derivation and bind checks |
| CL-FILESYSTEM-FINAL-COMPONENT-01 | On Unix, durable snapshot files/directories, evidence segment files/directories and cooperative lock files, and the Gate instance lock are opened with close-on-exec, no-follow, nonblocking, no-controlling-terminal flags and then type/identity checked. Final-component symlinks and writerless FIFOs therefore fail instead of being followed or awaited. Durable snapshot leaf operations additionally stay relative to one verified parent descriptor for each operation. The broader path-based primitives still require trusted ancestry and do not prevent replacement before/between operations, hostile rollback, non-cooperating writers, I/O stalls after a regular-file open, or power-loss loss. | PROVEN | `haldir-durable` snapshot symlink/FIFO/parent-path-replacement tests; `haldir-evidence` segment/manager symlink/FIFO and lock tests; `haldir-gate` instance-lock FIFO and exclusion tests |

## Maintenance

When you add a mechanism, add or update its `CL-` row here and cite the test or
CI gate that proves it. When you change wording in an authored doc that touches a
high-risk phrase, cite the relevant `CL-` id on that line so the drift guard can
see it is reconciled here. A claim with no evidence pointer must read PARTIAL,
PENDING, UNPROVEN, or OUT OF SCOPE — never PROVEN.
