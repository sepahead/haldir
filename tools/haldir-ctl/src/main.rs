//! haldir-ctl — offline inspection/verification tool.
#![forbid(unsafe_code)]

fn main() -> std::process::ExitCode {
    eprintln!("haldir-ctl: not yet wired in this scaffold");
    std::process::ExitCode::from(2)
}
