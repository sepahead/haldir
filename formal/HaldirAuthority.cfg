\* TLC configuration for HaldirAuthority (bounded, finite; runbook Phase 1/24).
\* Run in CI (.github/workflows/formal.yml). See formal/README.md.
CONSTANTS
  Epochs = {1, 2}
  MaxBoot = 3
  MaxGen = 3
  MaxSeq = 3

INIT Init
NEXT Next

INVARIANT Safety
